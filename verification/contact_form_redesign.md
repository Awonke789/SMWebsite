Contact form redesign verification

Desktop view: the left contact-information panel is dark navy with white text and light-blue icons. The form is visually separated with a 34px column gap and a blue border. The form heading appears above the fields, and each label sits above its input or textarea.

Mobile view: the contact panel stacks above the form, preserving readable spacing. The form border, heading, labels, and field controls remain visible within the mobile width.

Validation test: submitting the empty form produced the message "Please correct the highlighted fields before sending your message." and four field-level errors, including "Please enter your name." The form submission was prevented until required fields are corrected. Email format validation is also implemented.
